# Copy to .env and fill in. Never commit the real .env.
UNLOCKER_API_KEY=your_solving_api_key_here
PROXY=http://user:pass@host:port
# Optional: point at a different solver base URL (defaults to the hosted one).
# SOLVER_URL=https://solver.openscraper.ai
