# Copy to .env and fill in. Never commit the real .env.
OPENSCRAPER_API_KEY=sk_live_your_key_here     # https://openscraper.ai dashboard
PROXY=http://user:pass@host:port              # a sticky residential/ISP proxy
# Optional: override the API base (defaults to the hosted one).
# OPENSCRAPER_API_BASE=https://api.openscraper.ai
